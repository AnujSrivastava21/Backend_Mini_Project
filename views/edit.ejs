<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-black">
  <div class="bg-zinc-800 h-screen w-full   ">
    <form action="/updatePost/<%=post._id %>" method="post" class="w-full max-w-lg p-8 rounded-lg shadow-lg" name="content">
        <!-- Assuming "/create-post" is the endpoint to create a new post -->
        <div class="mb-4">
            <label for="postContent" class="block text-sm font-medium mb-4 mt-10 text-white">Edit Your post:</label>
            <textarea id="postContent" name="postContent" placeholder="What's on your mind?" rows="4"
            class=" bg-zinc-800 w-full px-3 py-2 border-2 rounded-xl mt-3  text-black"
            required> <%=post.content%></textarea>
        

        <button type="submit" class="bg-yellow-500 hover:bg-yellow-700 text-black font-bold py-2 px-4 rounded">Update Post</button>

    </form>
  </div>
</body>
</html>
